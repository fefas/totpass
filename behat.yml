default:
    autoload:
        '': %paths.base%/tests/features/bootstrap
    suites:
        default:
            paths:
                - %paths.base%/tests/features
            contexts:
                - CliContext:
                    workDir: %paths.base%/bin
                - TotPasswordContext
